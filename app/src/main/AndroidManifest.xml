<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cse.csenitd">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".openingActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".profile1"
            android:theme="@style/ProfileTheme" />
        <activity
            android:name=".question.questionsActivity"
            android:theme="@style/quesTheme"
            android:label="@string/title_activity_questions"/>
        <activity android:name=".question.quesdetail.questiondetailActivity"
            android:label="@string/title_activity_questionsdetail"/>
        <activity android:name=".ACHIEVEMENTS.Acheivements"
            android:label="Achievements"/>
        <activity android:name=".ACHIEVEMENTS.Add_achievement"
            android:label="Cse"/>
        <activity android:name=".Timeline.Add_Timeline"/>
        <activity android:name=".NoticeBoard.Notices"/>
        <activity android:name=".NoticeBoard.Add_Notices"/>
        <activity android:name=".Timeline.TimelineP"/>
    </application>

</manifest>